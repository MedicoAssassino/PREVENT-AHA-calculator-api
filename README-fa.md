# 🫀 وب‌سرویس (API) محاسبه‌گر ریسک PREVENT انجمن قلب آمریکا ۲۰۲۳

🌍 *مطالعه به زبان‌های دیگر: [English](README.md)*

یک سرویس API **سریع**، **ساختاریافته** و **مستقل** مبتنی بر **PHP** برای محاسبه خطر ۱۰ ساله ابتلا به بیماری‌های قلبی عروقی (CVD)، بیماری‌های قلبی عروقی آترواسکلروتیک (ASCVD) و نارسایی قلبی (HF) با استفاده از **معادلات AHA 2023 PREVENT™**.

---

## 🩺 اهمیت و پیش‌زمینه بالینی
معادلات **PREVENT** (پیش‌بینی خطر رویدادهای بیماری قلبی عروقی) جایگزینی به‌روز برای معادلات قدیمی‌تر *Pooled Cohort Equations (PCE)* هستند. 
این معادلات جدید، از داده‌های جدیدترین گروه‌های چندقومیتی استفاده می‌کنند و قابلیت این را دارند که نشانگرهای عملکرد کلیه (`eGFR` و `uACR`) و همچنین نشانگرهای متابولیک (`HbA1c`) را برای افزایش دقت پیش‌بینی در محاسبات لحاظ کنند.

📚 *منبع: جداول S12A تا S12E از ضمیمه مقاله انجمن قلب آمریکا (AHA) در سال ۲۰۲۳.*

---

## 🚀 راهنمای استفاده از API

این سرویس برای دریافت اطلاعات بالینی بیمار از طریق یک بدنه `JSON` طراحی شده است و پس از پردازش، درصدهای خطر ۱۰ ساله را بازمی‌گرداند.

### 🎯 ساختار مورد انتظار (Endpoint Expectation)
شما می‌توانید این کلاس را به سادگی در کنترلرهای فریم‌ورک‌هایی مانند (یا توابع Serverless) ادغام کنید تا به درخواست‌های `POST` پاسخ دهد.

**📌 هدرهای ضروری (Headers):**
- `Content-Type: application/json`
- `Accept: application/json`

---

### 📥 بدنه درخواست (Request Body)
در اینجا یک نمونه کامل از پارامترهای بالینی بیمار که باید در قالب JSON ارسال شوند، آورده شده است:

```json
{
  "gender": "male",
  "age": 60,
  "tc": 200,
  "hdl": 45,
  "sbp": 140,
  "bmi": 28.5,
  "egfr": 75,
  "diabetes": 0,
  "smoker": 0,
  "bpMeds": 1,
  "statin": 1,
  "hba1c": 5.8,
  "uacr": 15
}
```

> 💡 **نکته مهم:** پارامترهای `hba1c` و `uacr` **اختیاری** هستند. سیستم به گونه‌ای طراحی شده است که اگر این مقادیر ارسال نشوند، به صورت خودکار از مدل‌های پایه (Base Models) برای محاسبه استفاده می‌کند.

---

### 📤 پاسخ سیستم (Response)
پس از پردازش، خروجی به این شکل بازگردانده می‌شود:

```json
{
  "status": "success",
  "data": {
    "cvd": 12.4,
    "ascvd": 9.8,
    "hf": 3.5
  },
  "message": "Calculated successfully."
}
```

---

## 🧠 منطق و مسیریابی مدل‌ها (Routing Logic)
متد `calculate()` در این سرویس به عنوان یک مسیریاب هوشمند عمل می‌کند و مدل مناسب را بر اساس داده‌های ارسالی انتخاب می‌کند:

* 🥇 **مدل کامل (Full Model):** در صورتی که هر دو مقدار `hba1c` و `uacr` در دسترس باشند.
* 🥈 **مدل ACR:** در صورتی که فقط مقدار `uacr` ارسال شده باشد.
* 🥉 **مدل A1C:** در صورتی که فقط مقدار `hba1c` ارسال شده باشد.
* 🏅 **مدل پایه (Base Model):** در صورتی که هیچ‌کدام از این دو پارامتر اختیاری ارسال نشوند.

---

## 📦 نصب از طریق کامپوزر (Composer)

شما می‌توانید این پکیج را به راحتی از طریق کامپوزر در پروژه لاراول خود نصب کنید:

```bash
composer require medicoassassino/prevent-calc-api
```

### ⚙️ ادغام با لاراول

این پکیج از قابلیت **Auto-Discovery** لاراول پشتیبانی می‌کند، بنابراین نیازی به تنظیمات اضافی نیست و `Service Provider` و `Facade` به صورت خودکار در هسته لاراول ثبت می‌شوند.

### 🚀 مثال استفاده (با استفاده از Facade)

ساده‌ترین راه برای استفاده از این سرویس محاسباتی در کنترلرهای شما، فراخوانی Facade اختصاصیِ `PreventCalc` است:

```php
use HClean\PreventCalcApi\Facades\PreventCalc;

$data = [
    "gender" => "male",
    "age" => 60,
    "tc" => 200,
    "hdl" => 45,
    "sbp" => 140,
    "bmi" => 28.5,
    "egfr" => 75,
    "diabetes" => 0,
    "smoker" => 0,
    "bpMeds" => 1,
    "statin" => 1
];

$results = PreventCalc::calculate($data);
```

### 🛠 ثبت دستی (اختیاری - برای نسخه‌های قدیمی لاراول)

اگر از نسخه‌های قدیمی لاراول استفاده می‌کنید که قابلیت Auto-discovery را ندارند، باید پکیج را به صورت دستی در فایل `config/app.php` معرفی کنید:

```php
'providers' => [
    // ...
    HClean\PreventCalcApi\MyServiceProvider::class,
],

'aliases' => [
    // ...
    'PreventCalc' => HClean\PreventCalcApi\Facades\PreventCalc::class,
],
```
